# CRUD_Rest_API_consumption
I created a simple CRUD application using Node.js and React to consume an API with the following routes:  Create a user;  List existing users;  Edit a user;  Delete a user.
